rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Read-only for public by default. Writes go through Cloud Functions/Admin.
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
